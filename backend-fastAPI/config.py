# config.py

# --- Параметры подключения к LM Studio ---
BASE_MODEL_URL = "http://localhost:1234/v1"
API_KEY = "lm-studio"  # Для локального сервера может быть любой строкой

# --- Настройки моделей ---
# Модель для генерации ответов (Chat LLM)
LLM_MODEL = "google/gemma-4-e4b"

# Специализированная модель для векторизации (Embeddings)
# Например: "nomic-embed-text-v1.5" или та же Gemma
EMBEDDING_MODEL = "nomic-embed-text-v1.5"

# --- Параметры хранилища (Базы данных) ---
# Реляционная БД для истории чата и логов (требование ЛР №6)
SQLITE_DB_PATH = "chat_history.db"

# Векторная БД для контекста из документов
CHROMA_DB_PATH = "./chroma_db"

# --- Параметры RAG (Retrieval-Augmented Generation) ---
# Размер фрагмента текста (в символах/токенах)
CHUNK_SIZE = 1000
# Перекрытие чанков для сохранения смысловой связи
CHUNK_OVERLAP = 150
# Количество извлекаемых релевантных кусков текста для ответа
TOP_K_DOCUMENTS = 10
# Лимит истории: количество сообщений (пара), -1 для безлимита
HISTORY_LIMIT = 5
# Режим работы: "llm" или "template"
RESPONSE_MODE = "llm"

# --- Настройки диалоговой системы ---
# Название или роль системы (можно менять под любую задачу)
SYSTEM_ROLE_NAME = "Интеллектуальный ассистент"

# Базовая инструкция для модели (Системный промпт)
SYSTEM_PROMPT = (
    f"Ты — {SYSTEM_ROLE_NAME}. Используй предоставленный контекст из документов "
    "и историю переписки, чтобы давать точные и краткие ответы. "
    # "Если информации в контексте недостаточно, отвечай на основе своих знаний, но делай пометку об этом."
    "Используй только предоставленный контекст из базы знаний. Если в контексте нет ответа, так и скажи."
    "Без необходимости не употребляй перечисления."
)

# Сообщение для системы помощи (требование ЛР №6)
HELP_MESSAGE = (
    "Я — диалоговая система с поддержкой контекстных документов. "
    # "Вы можете загружать PDF-файлы, и я буду отвечать на вопросы по их содержанию, учитывая историю нашего общения."
    "Я буду отвечать на вопросы по содержанию уже загруженных файлов, учитывая историю нашего общения."
)
